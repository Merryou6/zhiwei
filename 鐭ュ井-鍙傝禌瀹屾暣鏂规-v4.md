# 知微 · 参赛完整方案（v4）

**粤港澳大湾区 AI Coding 创新大赛 | 方向一：AI + 教学管理助手**

> **版本演进**
> - **v1**：产品概念与四步教学闭环 —— 最大漏洞是"掌握度从哪来"
> - **v2**：补上四路数据采集与多源证据融合、错误类型诊断、引导退出通道，效果验证改为独立外部指标
> - **v3**：**确立"学习空间"作为产品第一性结构**；修正知识库的权威来源与边界；划清模型调用与确定性计算的职责；把"聚焦"的边界从架构层还回燃料层；补齐数据契约与后端取舍纪律
> - **v4**：**从"方案"到"施工"** —— 按两人团队 + AI 主导开发落地范围；补齐题目库数据契约（v3 最大缺口）；八表扩为九表、接口冻结为 19 个；勘误归因示例数值；判卷权全部收归服务端；排期改甲乙双线

---

## 一、一页速览

| 项目 | 内容 |
| --- | --- |
| 作品名称 | 知微 —— 会追问的 AI 学习伴侣 |
| 参赛方向 | 方向一：AI + 教学管理助手 |
| 一句话定位 | 基于知识图谱归因的一对一定制 AI 学习伴侣 |
| **核心结构** | **学习空间**（空间 → 绑定知识库 → 承载数据） |
| 核心创新 | 学习空间结构 + 四路数据采集与证据权重融合 + 知识点级掌握度建模 + 错误类型诊断驱动的归因定位 + 多模态读题引导 |
| 目标用户 | 初中学生（首批锚定**初中数学 · 二次函数主线**） |
| 指定平台 | 腾讯 LearnBuddy |
| 技术栈 | React + TypeScript / 腾讯云 CloudBase / 大模型 + 视觉模型 |
| 部署方式 | 静态托管，零服务器成本 |
| 名字释义 | 见微知著——从一道错题，看到整个知识结构的缺口 |
| 核心承诺 | 不给你答案，给你方法；只补你真正不会的那一环 |

### v3 相对 v2 的六项实质修正

| # | 修正项 | v2 的问题 | v3 的结论 |
| --- | --- | --- | --- |
| 1 | **学习空间** | 多学科数据平铺在一张画像上，"学科切换器"只是界面切换 | 引入学习空间作为**数据隔离的外显边界**，空间绑定知识库 |
| 2 | **知识库权威性** | "知识点框架基于公开课程标准"，未点名来源 | **点名三部课标**，节点加 `source` 字段逐条溯源 |
| 3 | **聚焦边界** | "初高衔接"实为跨学段，摊子过大 | **架构通用、燃料聚焦**——知识图谱锚定初中数学单学段 |
| 4 | **模型职责** | 未明确哪些环节由模型决策 | **模型只做受约束的结构化输出，不做关键结论决策** |
| 5 | **数据契约** | 四路通路无统一 schema，存在四套结构风险 | 确立**八张表 + 统一证据事件 schema + `space_id` 第一位** |
| 6 | **后端取舍** | 未区分严谨度，认证缺位 | **两刀切**：算法线严谨（幂等/可复现/参数外置），对话线底线三条；认证做最薄版 |

### v4 相对 v3 的六项落地修正

> v3 回答"应该做什么"，v4 回答"两个人 + AI 怎么做完"。**配套施工文档四份已冻结**（`API_CONTRACT.md` / `PRD.md` / `ALGORITHM.md` / `DATA_SCHEMA.md`），本文与施工文档冲突时，**施工文档为准**。

| # | 修正项 | v3 的问题 | v4 的结论 |
| --- | --- | --- | --- |
| 1 | **题目库契约** | `sample_items` 全空、`item_bank` 无字段定义——出题量与双池隔离要求悬空（**v3 最大缺口**） | 补齐题目库 schema + **`distractors` 绑定典型错误**；配额 train≥5 / retest≥6 / 每知识点，**20 节点共 ≥220 题**，双池零重叠由校验脚本断言 |
| 2 | **范围收敛** | P0/P1/P2 共 21 项，两人 8 天（AI 主导）不可完成 | 四路采集收敛为**测评 + 试卷两路主力**（自报作先验、静默并入对话传图）；教师演示页改 PPT 静态图；第二学科骨架、PDF 导出、语音提问全部砍掉 |
| 3 | **数据结构修订** | 八张表，识别结果无处存放；baseline/retest 证据无区分字段 | **八表扩为九表**（新增 `recognitions` 试卷识别任务表）；`evidence_events` 新增 `mode` 字段支撑 ΔAccuracy 分组统计；所有 kp 引用统一**完整 id** |
| 4 | **归因示例勘误** | `suspect_scores: 0.72` 超出公式上界（`0.6^d × (1−m)`，d=2 时 ≤ 0.36） | 示例按公式修正为合法值；归因路径示例数值与算法自洽 |
| 5 | **判卷权收归服务端** | `attribution/verify` 由前端传对错布尔值——与"答案不下发前端"矛盾；测评 `correct` 存在反推泄漏 | **一切判卷在服务端**（查 `item_bank.answer`）；`correct` 仅测量模式（baseline/retest）下发，诊断模式恒为 null |
| 6 | **排期与验证样本** | 单人任务列表；验证样本 8–10 名 | 改为**甲（诊断线）/ 乙（教学线）双线纵切**，两人各自全栈；验证样本 **5 名起步**（招募顺利扩至 8–10），认可率判据 ≥75%（冲刺 80%） |

---

## 二、赛题理解

### 2.1 赛事命题意图

赛题以"学·用·创·赛"完整生命周期为理念，强调将创意转化为真实作品，并要求作品"具有完整功能、清晰的应用场景与良好的用户体验"。方向一原文的核心表述是"让 AI 成为师生的教学副驾驶"。

关键词是**副驾驶**。副驾驶不替你开车——它提醒你、纠正你、让你自己把车开好。

### 2.2 行业现状与我们的切入点

现有题库类产品的核心链路是"拍题 → 出答案"，解决的是**当下的作业焦虑**，代价是**长期的能力空心化**。

更关键的是，它们只能记录"这道题错了"，却无法回答真正有价值的问题：**他到底是哪里不会？**

一道二次函数最值题做错，可能是最值求法不会、配方不会、顶点式没记牢，甚至是一元二次方程判别式没掌握。传统工具把这四种情况一视同仁，于是补救方案只能是"多刷同类题"——对其中三种情况完全是无效劳动。

**知微要补的就是这一环。** 我们不记录"题错了"，我们回答"你到底是哪里不会"，并进一步回答"你是这个知识点的哪一种不会"。

### 2.3 我们的三个判断

**判断一：深度比广度重要。** 知识点覆盖数量不是壁垒，AI 应用深度才是。技术创新性占 30 分，铺得越宽每处越浅。

**判断二：归因能力是真正的技术差异点。** 掌握度建模 + 错误类型诊断 + 先修关系回溯，是可解释、可验证、有算法含量的能力。

**判断三：数据采集的可持续性决定产品能否成立。** 再好的算法，没有数据就是空转。**必须让学生"不需要额外付出"就能持续贡献数据。**

---

## 三、产品定位

### 3.1 一句话定义

**知微是一个基于知识图谱的 AI 学习伴侣：它在"学习空间"中承载学生的学习证据，通过四种低打扰的方式持续采集，定位真正的知识缺口与典型误解，沿着先修关系回溯出根因，生成专属补救路径，并用追问而非答案引导学生走完这条路。**

### 3.2 目标用户

| 用户 | 核心痛点 | 知微的解法 |
| --- | --- | --- |
| 初中学生 | 错题反复错，不知道补哪里 | 归因定位 + 专属补救路径 |
| 学生家长 | 看不出孩子薄弱在哪 | 可视化知识图谱与掌握度报告 |
| 教师 | 几十人无法逐个诊断 | 班级掌握度热力图（PPT 静态设计图，v4 不做真实页面） |

### 3.3 与竞品的本质差异

**这一节是答辩时的正面战场。** 评委心里真正的问题是："学生直接开通用 AI 聊天网页问不就行了？"

| 维度 | 通用 AI 聊天网页 | 传统题库类产品 | 知微 |
| --- | --- | --- | --- |
| 状态 | **无状态**，每轮对话从零开始 | 仅答题行为 | **有状态**：知识点级掌握度向量持续累积 |
| 核心链路 | 问 → 答 | 拍题 → 给答案 | 空间承接 → 采集 → 诊断 → 归因 → 处方 → 验证 |
| 对错题的理解 | 不知道你错了 | 这道题错了 | 定位知识点，并判定**属于哪一种错误** |
| 数据来源 | 无 | 仅答题行为 | 自报 + 测评 + 静默采集 + 试卷上传，**四路融合** |
| 学生画像 | 无 | 做题量、正确率 | 知识点级掌握度向量（0–1 连续值） |
| 反馈方式 | 直接输出答案 | 直接输出答案 | 苏格拉底式追问，带退出通道 |
| 学习路径 | 无 | 同类题推送 | 基于先修关系的动态路径规划 |
| 效果验证 | **不可能**（无基线、无记忆） | 分数变化 | 新题复测 + 教师认可率双指标 |

**汇报话术（建议直接采用）**：

> "我们不是比通用 AI 更聪明，我们做的是它在结构上做不到的事。**通用聊天网页解决的是'这道题怎么做'，知微解决的是'你到底哪里不会、为什么不会、下一步补哪里'。**"

**必须提前备好的两个反问：**

- **问**："这不都是 Prompt 工程吗？我塞一段长 Prompt 也能做。"
  **答**："Prompt 能做单次行为，做不了持续状态。掌握度向量、证据权重、题目池隔离、跨会话更新，这些需要数据库和引擎。而且通用网页无法保证行为一致性——同一个学生的第十次对话和第一次，模型行为会漂移，教学场景最怕这个。"
- **问**："那你自己不也是调大模型？"
  **答**："是，但大模型在知微里是**执行层不是决策层**。错误分类必须从 `typical_errors` 枚举里选，归因方向由映射表决定，引导节奏由 `progress` 标记控制。**模型负责'怎么说'，确定性和连续性由我们负责。**"

---

## 四、学习空间 ★v3 核心结构

**这是 v3 的第一性设计。** v2 的问题是：多学科的数据平铺在同一张画像上，所谓"学科切换器"只是 UI 层的切换，不是数据层的隔离。

### 4.1 三层结构

```
【云盘层】文件存储
   ├─ 📕 义务教育数学课程标准（2022年版）.pdf   ← 默认预置
   ├─ 📕 人教版九年级数学上册.pdf               ← 默认预置
   └─ ...（用户自己的文件）
        ↓ 形成
【知识库层】结构化解析结果
   └─ "初中数学"知识库（节点 + 先修关系 + 典型错误）
        ↓ 绑定
【学习空间层】使用场景与数据承载
   └─ "初中数学"空间
        ├─ 掌握度画像
        ├─ 证据事件
        ├─ 归因记录
        └─ 对话历史
```

**一句话讲清三层关系：云盘存文件，知识库是解析结果，空间是使用场景。**

### 4.2 空间与知识库：一对多

| 方案 | 含义 | 结论 |
| --- | --- | --- |
| **一对多** | 一个空间绑定一个知识库 | **采用** |
| 多对多 | 一个空间可挂多个知识库 | **不做** |

**为什么不做多对多**：会立刻带来"两个知识库的节点 id 冲突怎么办""先修关系跨库怎么连"两个致命问题。8 天工期不做。

**但留后门**：空间表中 `knowledge_source` 存为**数组**而非单值，将来升级为多选不需改表结构。

### 4.3 核心约束：一学科一空间

> **这是整个设计中最重要的一条产品规则。**

掌握度挂在空间上，意味着**同一学科开两个空间，掌握度就是两份，互相看不见**。这对学生是反直觉的，必须主动防范。

| 规则 | 实现 |
| --- | --- |
| **新建空间时检测同学科冲突** | 若已有同学科空间，弹提示"你已有一个初中数学空间，是否切换过去？"，默认按钮是**切换**而非新建 |
| **空间名默认 = 知识库名** | 如"初中数学"，**不提供自由命名**。自由命名是"多空间同学科"的最大诱因——一旦出现"我的数学小天地"和"数学冲刺"，学生一定会建两个 |
| **空空间不占学科槽** | "试卷自建"类无学科属性的空空间，可建多个，不触发冲突检测 |

### 4.4 默认预置（开箱即用）

学生**不应看到"创建空间"这个动作**。参照 4.2 漏斗式采集的思想——让用户不需要为了"让系统更好用"而专门付出。

```
注册
 └→ 自报问卷时顺带问"你主要想补哪一块？"
      └→ 自动创建空间（默认名 = 学科名，如"初中数学"）
           └→ 自动绑定内置知识库（如 math/cz）
                └→ 自动关联云盘中预置的课标与教材（作为对照参考资料）
                     ↓
              学生直接进入测评，全程无感知
```

**"新建学习空间"作为空间列表页的次要按钮存在，不占首屏。**

**默认预置清单：**

| 项目 | 内容 |
| --- | --- |
| 云盘预置文件 | 《义务教育数学课程标准（2022年版）》、《人教版九年级数学上册》 |
| 默认知识库 | "初中数学"知识库（对应 `data/knowledge/math/cz.json`） |
| 默认空间名 | 初中数学（与知识库同名） |
| 绑定的知识 | 初中数学 20 个知识点（全部配满 `typical_errors`） |

### 4.5 自定义知识库：占位，不开放

**为什么不做真实上传**——这里必须澄清一个易被误判的类比：

| 维度 | 上传试卷（通路二） | 上传知识库 |
| --- | --- | --- |
| 文件性质 | 一次性消耗品 | **长期依赖的运行时底座** |
| 进入系统后 | 识别完变成 `evidence_events` 记录 | **变成图谱节点，供所有后续分析反复读取** |
| 格式要求 | 宽松——识别不清可让学生手点 | **极严**——节点 id、`prerequisites` 引用必须自洽 |
| **出错后果** | **一条证据错，扣一次掌握度** | **整个空间废掉**——先修关系断裂，归因全部失效 |
| 需要校验吗 | 不需要 | **需要，且必须强校验** |

**关键差别**：试卷是**证据**，知识库是**底座**。试卷传错删掉重传即可；知识库传错，`prerequisites` 指向不存在 id → 拓扑剪枝失效 → 归因回溯走到死路 → **整个空间诊断能力静默失效**。而且**它不会报错，只会给出错误结果**，这是最难排查的故障类型。

**实现方式（禁用态，非假文件）**：

```
知识库上传入口：按钮置灰 + 文案"自定义知识库即将开放"
```

**不要建 0 字节的同名文件**——它会在云盘里留下一个损坏的假文件，学生点开发现打不开，反而显得产品有 bug。**禁用态比假文件干净。** 这一条在 PPT 中作为"后续规划"呈现。

### 4.6 空间与知识库关系的诚实表述

评委一定会问："内置知识库和云盘里的教材是什么关系？"

| 关系 | 含义 | 结论 |
| --- | --- | --- |
| 内置知识库是**独立数据** | 云盘教材仅为对照参考资料，与图谱无自动依赖 | **采用，且坦诚说明** |
| 内置知识库是**从教材解析出来的** | 需要真实 PDF 解析 | 不做，写入"未来方向" |

**表述建议**：知识库来自**课程标准的结构化整理**（模型辅助生成 + 人工校验），云盘教材作为**对照参考资料**提供给学生。

> ⚠️ **切忌**声称"我们自动解析了教材生成了知识图谱"——评委翻一下节点数据就知道对不上，反而扣分。**坦诚的边界比夸大的能力更得分。**

---

## 五、数据生产与消费 ★v3 新增

**这一章回答两个问题：数据从哪来（生产者），谁在用（消费者）。**

### 5.1 六个一手数据生产点

| # | 生产者 | 产生的数据 | 载体 | 数据性质 | 位置 |
| --- | --- | --- | --- | --- | --- |
| 1 | **学生**（作答行为） | 对错结果、作答时长 | 测评页 | 强观测 `w=1.00` | 6.1 通路三 |
| 2 | **学生**（对话传图读题） | 题目图片、知识点匹配事件 | 对话辅导页（静默采集，无独立入口） | 弱负证据 `α=0.10` | 6.1 通路一 |
| 3 | **学生 + 教师**（批改试卷） | 手写作答、批改符号、题目切分 | 试卷上传 | 强观测 `w=0.80` | 6.1 通路二 |
| 4 | **学生**（自报问卷） | 5 档主观掌握度 | 注册/进章节 | 先验，**不作观测** | 6.1 通路四 |
| 5 | **视觉模型**（自动识别） | 题干、公式、图形的结构化结果 | 链路 A/B | 中间产物 | 11 章 |
| 6 | **智能体自身**（对话中） | 每轮 `progress` 标记 | 对话辅导页 | 触发退出通道 | 9.2 |

**第 6 项容易被忽略但很关键**：引导对话过程本身也在生产数据，这是"把教学反馈接回诊断闭环"的实现——**诊断与教学不是单向管道，是双向循环**。

### 5.2 派生数据（消费的产物，又回流为一手证据）

- **错误类型判定**（结构化 JSON：`error_type` / `matched_typical_error` / `confidence`）
- **归因结论**（根因知识点 + 回溯路径）
- **学生反驳信号**（"我不认同这个判断"按钮）

**这三类说明数据链是循环，不是单向。**

### 5.3 消费者与主线数据流

| 消费者 | 消费什么 | 产出什么 |
| --- | --- | --- |
| 加权 BKT 引擎 | 四路观测 + 权重 `w` | 更新后的掌握度向量 |
| 错误诊断器 | 学生具体错误答案 + `typical_errors` | 结构化错误判定 |
| 归因引擎 | 错误类型 + 图谱先修关系 | 根因假设 + 验证题 |
| 智能体对话 | 掌握度 + 根因 + 错误类型 | 分层提示、退出通道 |
| 学生 / 家长 | 掌握度分布、缺口清单 | 可视化报告 |
| 教师 | 班级掌握度热力图 | 演示页（不实现真实班级逻辑） |

```
生产者（学生/教师/视觉模型）
   ↓  四路原始证据
证据融合引擎（加权 BKT，w 决定信任度）
   ↓  掌握度向量
错误诊断器 → 归因引擎
   ↓  根因 + 错误类型
处方生成 + 智能体对话
   ↓  progress 标记回流
────────── 回到生产者（循环） ──────────
```

### 5.4 生产端的两处结构性隐患 ★必须正视

**隐患一：视觉模型识别质量（高）**

手写与批改符号识别是真实难点，靠"识别 + 人工确认"双保险兜底。**缓解措施已有，风险中高。**

**隐患二：`progress` 标记的判定者问题（高）**

`progress` 标记用来自动触发退出通道，但 v2 未说明它由谁判定。**如果由模型自判，则判断者 = 被验证者，存在自证问题。**

**v3 的解法**：要求模型**每轮对话同时输出结构化 `progress` 字段，并附一句判断依据**，代码读取字段而非解析自然语言。

```json
{
  "progress": false,
  "progress_reason": "学生仍在猜测顶点坐标的符号，未触及'括号内取反'这一关键规则"
}
```

**同时增加反向校验**：用学生下一题的作答表现回溯验证 `progress` 标记的准确性，避免在学生其实会的情况下误触发退出、把他退回上游。

---

## 六、掌握度的四路采集与多源证据融合

### 6.1 四条数据通路

#### 通路一：静默采集（对话传图读题）

学生在对话辅导页上传题目图片求解时，系统自动识别题目涉及的知识点并记录，**全程不打扰**。v4 收敛范围后**不再设独立"拍照搜题"入口**——传图读题本身即采集动作，识别置信度 ≥ 0.6 时产生弱负证据。采集后仅用一个轻量气泡做非侵入式确认："这道题考的是二次函数最值，你是在做作业，还是想补这一块？"

- **优点**：零额外成本，数据量随使用自然增长
- **缺点**：噪声大（可能是随手拍、可能是家长拍）
- **建模方式**：产生**弱负证据**。学生求助本身暗示"大概率未掌握"，用小幅修正：`P(L) ← P(L) × (1 − α)`，其中 `α = 0.10`
- **设计要点**：一口气问十道题，不应让掌握度暴跌。**同类证据在时间窗口内需去重合并**（同一知识点 1 小时内只计一次）

> **这不是技术细节，是产品态度**：惩罚求助行为的产品，学生会立刻停止求助。

#### 通路二：试卷 / 答题卡上传

学生上传已批改的试卷或答题卡照片。

- **优点**：**一次上传 = 一次全章诊断**。一张试卷可覆盖 10–20 个知识点，且带有真实作答与教师判定，是**单位成本信息量最高**的通路
- **缺点**：手写识别与批改符号识别难度大
- **建模方式**：产生**强观测**，进入加权 BKT 更新，`w = 0.80`
- **工程折中**：设计**"识别 + 人工确认"双保险**——系统识别出题目与作答后，由学生点选哪些对了、哪些错了。成本极低、鲁棒性极高，且这个交互本身让学生有参与感
- **兜底话术**：识别不清时不硬猜，明确提示"这道题我没看清，麻烦你手动标一下对错"

#### 通路三：开放测评系统

学生主动进入测评，系统用少量基础题给出掌握度。

- **优点**：证据质量最高，是冷启动阶段的主力
- **建模方式**：**强观测**，`w = 1.00`（标定基准）
- **选题策略**：自适应用题——每题选"当前掌握度最接近 0.5"的知识点，因为那里信息增益最大。收敛判据为后验方差低于阈值或题量达 8–12 题
- **设计要点**：诊断阶段**不即时反馈对错**，避免学生反推答案、污染数据

#### 通路四：学生自报

注册或进入新章节前，用一个极简问卷建立先验：

| 学生的回答 | 初始掌握度 P(L0) |
| --- | --- |
| 完全没学过 | 0.10 |
| 学过，但基本不会 | 0.30 |
| 学过，一般般 | 0.50 |
| 学过，比较熟 | 0.70 |
| 很熟，能给别人讲 | 0.85 |

- **优点**：10 秒完成，覆盖全学科，解决冷启动
- **缺点**：存在自我认知偏差（高估与低估都常见）
- **设计要点**：自报**只作为先验，不作为观测**——一旦有真实答题证据，权重迅速被真实数据覆盖。自报的价值在于"起点比 0.3 更聪明"，而不是"取代测量"

### 6.2 漏斗式采集策略

四条通路不是并列的，而是按**"先便宜后贵、先粗后细、永不打扰"**的原则排成漏斗：

```
注册时        通路四 学生自报     → 建立先验 P(L0)
   ↓ 10 秒
开课初        通路三 快速测评     → 核心章节收敛（8–12 题）
   ↓ 2 分钟
日常使用      通路一 对话传图（静默） → 持续微调
   ↓ 零成本
每周一次      通路二 试卷上传     → 批量校准
```

**这个顺序本身就是产品设计的一部分**：用户永远不需要为了"让系统更懂我"而专门付出。数据是在他正常使用的过程中自然产生的。

### 6.3 多源证据融合

不同来源的可靠性不同，必须区别对待：

| 来源 | 证据类型 | 覆盖率 | 噪声 | 权重 |
| --- | --- | --- | --- | --- |
| 学生自报 | 先验设定 | 全学科 | 高（自我认知偏差） | 设定 `P(L0)` |
| 对话传图读题（静默） | 弱负证据 | 随使用增长 | 高（可能随手拍） | `α = 0.10` |
| 拍照上传批改试卷 | 强观测 | 按章节批量 | 中（识别误差） | `w = 0.80` |
| 系统测评答题 | 强观测 | 精准但窄 | 低 | `w = 1.00` |

**加权 BKT 更新公式（三段式）**：

1. **贝叶斯更新**——按标准 BKT 计算观测后验：

   答对：`P_obs = P(L)(1−P(S)) / [ P(L)(1−P(S)) + (1−P(L))P(G) ]`

   答错：`P_obs = P(L)P(S) / [ P(L)P(S) + (1−P(L))(1−P(G)) ]`

2. **按证据权重插值**——得到有效后验：

   `P_eff = w · P_obs + (1 − w) · P(L)`

3. **叠加学习迁移**——反映"练一次总有一点进步"：

   `P_new = P_eff + (1 − P_eff) · P(T)`

**这个设计的优雅之处**：`w = 1` 时完整退化为标准 BKT，`w = 0` 时完全不采纳该证据。权重就是一个可解释的"我有多信你"旋钮。参数默认 `P(S)=0.1`、`P(G)=0.2`、`P(T)=0.15`。

**为什么用 BKT 而不是正确率统计**：正确率无法区分"不会但蒙对"和"会但不小心错"，而 BKT 通过猜测率与失误率两个参数把这两种噪声显式建模了。

**v3 补充——这五个参数全部学科无关**：

| 参数 | 是否需按学科隔离 |
| --- | --- |
| `P(S)` / `P(G)` / `P(T)` / `α` / `w` / `d`（回溯跳数） | **全部不需要**，全局共用 |

**这一点很重要**：它说明"通用性"不需要牺牲"聚焦"，参数层完全共享，只有知识内容层需要分文件。

### 6.4 冷启动的补充机制：上游探测

即使有自报和测评，仍有一个残留问题：**新用户的第一份掌握度估计方差极大**。

**解决方案**：利用先修关系天然构成**拓扑序**这一性质，初诊**从上游开始探测**，而非从学生正在学的章节开始。

规则：若某知识点的所有先修节点掌握度均低于阈值（0.4），则该知识点直接标记为"未具备学习条件"，**不再出题测量**（因为必然不会，测了是浪费）。

这条规则用**结构先验替代测量**，能在极少的题目内得到比纯测量更可靠的画像。教育测量学中对应的成熟方法是**属性层级方法（Attribute Hierarchy Method）**。

---

## 七、错误类型诊断

### 7.1 五类错误分类体系

学生答错同一道题，原因可能完全不同。只看"对/错"会丢掉最值钱的信息：

| 错误类型 | 识别特征 | 教学含义 |
| --- | --- | --- |
| 前修缺失 `prerequisite_gap` | 使用上游知识时出错 | 真正的洞在上游 |
| 概念混淆 `concept_confusion` | 定义、定理、性质用错 | 该知识点本身没懂 |
| 方法缺失 `method_gap` | 思路方向错误、无从下手 | 缺方法，不是缺概念 |
| 符号失误 `procedural_slip` | 步骤正确但计算/符号错 | 熟练度不足 |
| 审题偏差 `misreading` | 误读条件、漏看要求 | 非知识问题 |

### 7.2 错误类型决定归因方向（关键设计）

v1 的算法是"错题一律沿先修关系往上游回溯"，这会产生**过度归因**：把一次纯粹的计算失误也归因到三跳之外的知识点，给出的补救方案反而误导学生。

修正后的逻辑是**先判类型、再定方向**：

| 错误类型 | 归因方向 | 对应处方 |
| --- | --- | --- |
| 前修缺失 | → 沿先修关系回溯，找真正断掉的上游节点 | 跳转上游补课 |
| 概念混淆 | → **根因即本节点**，不回溯 | 重讲概念 + 对比辨析 |
| 方法缺失 | → **根因即本节点**，不回溯 | 讲方法 + 同类示范 |
| 符号失误 | → **不进入知识归因** | 限时熟练度训练 |
| 审题偏差 | → **不进入知识归因** | 审题训练：划关键词 |

**这张表本身就是产品价值的证明**：它说明知微不是简单地"错题推同类题"，而是**因为不同的病因而给不同的药**。一次符号失误，绝不该让学生重听一遍概念讲解——那是在浪费他的时间。

### 7.3 实现方式

让智能体读取学生的**具体错误答案**（文本或图片中的作答内容），输出结构化判定：

```json
{
  "knowledge_point": "math.cz.quadratic.vertex_form",
  "error_type": "concept_confusion",
  "matched_typical_error": "sign_confusion",
  "confidence": 0.82,
  "evidence": "学生把 y=(x+2)²+3 的顶点写成 (2,3)，说明未掌握顶点式中 h 的符号规则",
  "attribution_direction": "self"
}
```

这个结构化输出同时喂给三个下游：`error_type` 决定归因方向，`matched_typical_error` 用于生成针对性讲解，`confidence` 影响掌握度更新权重。

**三条硬约束（详见附录 B）**：

1. 必须从 `typical_errors` 列表里选，**不允许模型自由发明错误类型**
2. `confidence < 0.6` 时**不采纳**该判定，退回向学生追问澄清
3. 先让模型复述"学生具体错在哪一步"，**再**判定类型——强制先观察后判断

---

## 八、四步教学闭环（修订版）

**智能诊断 → 归因定位 → 生成处方 → 效果验证**，每轮结束后掌握度更新，自动进入下一轮。

### 8.1 智能诊断

**输入**：空间内的最新掌握度向量 + 待诊断的知识范围
**输出**：一组带证据权重的观测，用于更新掌握度

流程：
1. **拓扑剪枝**——按 6.4 的规则，先排除所有先修未达标的知识点，不再出题测量
2. **自适应选题**——在剩余知识点中，每题选掌握度最接近 0.5 的（信息增益最大）
3. **收敛判定**——后验方差低于阈值或题量达上限（8–12 题）

### 8.2 归因定位

**第一步：错误类型判定。** 调用 7.3 的结构化诊断，得到 `error_type`。

**第二步：按类型分流。**

- 若为 `procedural_slip` 或 `misreading` → **不进入知识归因**，转交熟练度/习惯训练模块，流程结束
- 若为 `concept_confusion` 或 `method_gap` → 根因即当前知识点，跳过回溯，直接进入处方
- 若为 `prerequisite_gap` → 进入第三步

**第三步：先修回溯。** 以当前知识点为起点，沿先修关系**反向**搜索，深度 `d` 限制在 2–3 跳内，避免归因过度发散。对每个候选知识点 `a` 计算嫌疑分：

`suspect(a) = w(d) × (1 − mastery(a))`，其中 `w(d) = 0.6^d`

距离越近、掌握度越低者嫌疑越大。

**第四步：假设验证。** 取嫌疑分最高者为根因假设，针对它出一道**最小验证题**。答对则确认并更新掌握度；答错则排除假设，取次高嫌疑者重新验证。

**第五步：写入画像。** 确认的根因连同错误类型一起进入学生知识画像。

**为什么这个算法站得住脚**：**可解释**（每一步回溯路径可展示）、**可验证**（有假设验证，不瞎猜）、**可迭代**（掌握度随每次答题更新）、**有前置判断**（错误类型避免了过度归因）。

### 8.3 生成处方

根据根因知识点 + 错误类型，生成三件事：

1. **针对性讲解**：按错误类型选择讲解策略（概念混淆→对比辨析；方法缺失→思路示范；前修缺失→先补上游）
2. **巩固题序列**：从根因出发沿先修关系**正向**排列，难度递增
3. **路径可视化**：在知识图谱上高亮这条路径，让学生知道"要走几步、走哪几步"

### 8.4 效果验证（独立外部指标）

v1 用"掌握度提升 ≥ 0.2"作为验证标准，存在**自我循环论证**的问题——掌握度本身就是我们模型算出来的，等于自己给自己打分。改用**独立外部指标**：

**指标一：新题正确率提升**

- 复测题目池与训练题目池**严格隔离**（题目 ID 零重叠，由校验脚本断言，不靠人工核验）
- `ΔAccuracy = 复测新题正确率 − 基线新题正确率`
- 目标示例：**从 33% 提升到 80%**（⚠️ 此为设计示例，PPT 与答辩**一律使用实测值**，禁止照搬）
- 判据：`ΔAccuracy ≥ 0.3` 且复测正确率 ≥ 0.75，判定缺口已修复

这个指标的好处是**它和你的模型无关**。无论掌握度怎么算，学生在新题上做对了多少，是客观事实。答辩时说"复测新题正确率从 33% 提升到 80%"，评委一听就懂；说"掌握度提升 0.2"，评委得先信你的模型。

**指标二：归因认可率**（见第十六章）
**指标三：引导有效性**（见第十六章展望）

---

## 九、对话引导策略（含退出通道）

### 9.1 分层提示

1. **先定位**：确认学生卡在哪一步，不急于讲解
2. **先问后讲**：优先用提问推进
3. **分层降级**：学生连续两次答不上，给方向性提示；三次以上才给完整解法
4. **默认不给答案**：即使学生直接要答案，也先引导一次；同时提供"我确实想直接看解法"的出口——尊重用户，但把引导作为默认路径
5. **结束要回收**：生成一句"你这次的收获"总结，沉淀到学习记录

### 9.2 退出通道

分层提示漏掉了一种情况：**学生反复答不上来，其实说明前修缺失了**——不是他不会这个知识点，而是他连这个知识点的地基都没有。此时继续在原地提示，只会造成持续挫败。

**规则**：

- 智能体每轮对话输出一个内部的 `progress` 标记（v3：**结构化字段 + 判断依据**，见 5.4）
- **连续 3 轮 `progress = false`** → 触发退出通道
- 触发后执行四个动作：
  1. **暂停**当前知识点，状态标记为 `blocked_by_prerequisite`
  2. **回溯**：沿先修关系找最近的、掌握度低于 0.6 的上游节点
  3. **告知**：明确告诉学生"我们先往回看一眼 XX，那里可能是真正卡住你的地方"（坦诚，不让学生以为是自己笨）
  4. **重启**：在上游节点重新开始引导
- **跳转上限**：连续跳转不超过 2 层，防止一路滑到底
- **v3 新增反向校验**：用学生下一题的实际作答表现，回溯验证 `progress` 标记的准确性

**这个机制的价值**：它把**教学反馈接回了诊断闭环**。原本"诊断 → 教学"是单向的，退出通道让它变成双向——教学过程本身成为发现知识缺口的手段。**这本身就是一个独立创新点。**

### 9.3 归因结论可反驳

系统说"你的根因是顶点式"，但学生明明懂、只是看错题了怎么办？

在归因结果页提供**"我不认同这个判断"**按钮，学生的反馈进入再验证流程（追加一道验证题，或直接调整该证据权重）。这既是体验优化，也是数据质量优化。

---

## 十、知识图谱设计 ★v3 强化权威性

### 10.1 节点模型（含来源溯源）

```json
{
  "id": "math.cz.quadratic.vertex_form",
  "name": "二次函数的顶点式",
  "subject": "数学",
  "stage": "初中",
  "grade": "九年级",
  "chapter": "二次函数",
  "difficulty": 3,
  "source": {
    "standard": "义务教育数学课程标准（2022年版）",
    "item": "第三学段·数与代数·函数·二次函数",
    "type": "derived"
  },
  "prerequisites": [
    "math.cz.quadratic.completing_square",
    "math.cz.function.graph"
  ],
  "prerequisite_basis": "教材章节顺序 + 学科逻辑推导，非课标直接规定",
  "successors": ["math.cz.quadratic.extremum"],
  "typical_errors": [
    {
      "code": "sign_confusion",
      "desc": "顶点坐标 h、k 的符号混淆",
      "error_type": "concept_confusion",
      "remedy": "用 y=(x−h)²+k 与 y=(x+2)²+3 对照，强化'括号内取反'的规则"
    }
  ],
  "sample_items": ["q_cz_vertex_001", "q_cz_vertex_002", "q_cz_vertex_003"]
}
```

**v3 新增两个字段，用于权威溯源：**

| 字段 | 作用 |
| --- | --- |
| `source` | 逐节点标注课标来源与映射类型（`direct` 直接对应 / `derived` 推导） |
| `prerequisite_basis` | **明确先修关系是推导而非引用**——课标不直接规定先修关系，坦诚反而加分 |

`prerequisites` 与 `successors` 构成**有向无环图（DAG）**，这是归因与拓扑剪枝的基础设施。`typical_errors` 是错误类型诊断的匹配目标。

### 10.2 权威来源清单

**v2 只说"知识点框架基于公开课程标准"，从未点名。这是全篇最大的权威性缺口**——评审会问"你的知识点图谱凭什么说是对的？"

| 权威来源 | 发布方 | 对方案的价值 |
| --- | --- | --- |
| **义务教育数学课程标准（2022 年版）** | 教育部 | 初中数学知识点与认知要求的**唯一权威依据**，图谱一级来源 |
| 普通高中数学课程标准（2017 年版 2020 年修订） | 教育部 | 高中承接节点的依据 |
| 义务教育信息科技课程标准（2022 年版） | 教育部 | 第二学科切片的依据 |
| 人教版 / 北师大版教材目录 | 人民教育出版社等 | 章节顺序与先修关系可对照教材编排 |
| 中国高考评价体系 | 教育部考试中心 | "必备知识 / 关键能力 / 学科素养"层级，可作先修关系的理论映射 |
| TIMSS / PISA 数学框架 | IEA / OECD | 国际对标，体现"不是闭门造车" |

**核心动作：把知识图谱从"模型生成"改为"课标映射 + 模型辅助"。**

1. **确立唯一权威基线**——明确写"知识点以《义务教育数学课程标准（2022 年版）》为唯一权威来源"，节点与课标内容条目的映射关系逐条标注在 `source` 字段
2. **先修关系分离来源**——明确标注为"依据教材章节顺序 + 学科逻辑推导"
3. **`typical_errors` 降级表述**——典型错误库本质是经验数据，无权威标准可依，坦诚写"来自教学经验与文献归纳，非权威标准"，**同时这正是教育专业队友的实质产出**

### 10.3 掌握度模型

采用**贝叶斯知识追踪（BKT）**，四参数：

- `P(L0)` 初始掌握概率——由自报设定（见 6.1 通路四）
- `P(T)` 学习迁移率 = 0.15
- `P(G)` 猜测率 = 0.20
- `P(S)` 失误率 = 0.10

更新公式见 6.3 的加权三段式。

掌握度按区间映射为四个状态用于界面呈现：`<0.4` 待巩固、`0.4–0.6` 不稳定、`0.6–0.8` 基本掌握、`>0.8` 已掌握。

### 10.4 可扩展性设计：架构通用，燃料聚焦 ★v3 修正

知识点数据与代码**完全解耦**。每个学科的知识点存于独立 JSON 文件 `data/knowledge/{subject}/{stage}.json`。新增学科或学段**只需新增数据文件，不改任何算法与前端代码**。

**v3 必须澄清的一个误解：聚焦不是技术约束。**

| 模块 | 依赖知识内容吗 | 依据 |
| --- | --- | --- |
| 加权 BKT 引擎 | **不依赖** | 6.3 公式只吃 `P(L)` 和 `w` |
| 错误类型诊断 | **不依赖** | 7.1 五类错误是通用分类 |
| 归因算法 | **不依赖** | 8.2 只吃图谱结构和掌握度 |
| 拓扑剪枝 | **不依赖** | 6.4 只判断先修节点掌握度是否 < 0.4 |
| 退出通道 | **不依赖** | 9.2 只看 `progress` 与先修关系 |
| 对话引导 | **部分依赖** | 讲解内容需贴合知识点，骨架与学科无关 |

**唯一真正"贵"的是 `typical_errors`**——它无法自动生成，需要真正懂教学的人逐条判断"这个错误对应哪类病因、该怎么救"。

| 项目 | 数量 | 成本 |
| --- | --- | --- |
| 知识点 | 20 个 | 模型批量生成 + 人工核对，快 |
| **典型错误** | **每节点 3–5 条，共 60–100 条** | **每条都要：识别特征 + 教学含义 + remedy 话术** |

**所以聚焦的边界不是"引擎能跑多远"，而是"燃料能烧多旺"：**

| 层次 | 策略 |
| --- | --- |
| **引擎层** | **完全不设限**——数据格式定好，想加多少学科就加多少 |
| **燃料层** | **集中火力**——20 个知识点全部配满 `typical_errors`，题目配额 train≥5 / retest≥6，共 ≥220 题 |
| **验证层** | **样本同质**——5 名学生起步（扩至 8–10）+ 教师审阅全部落在同一章节 |
| **其他学科** | **v4 不做骨架**——"引擎无 `if (subject)` 分支"这一事实本身就是可扩展性证明；PPT 口头说明架构可扩展，不展示半成品数据 |

**v3 结论：引擎做通用，燃料做聚焦。** 采用"**初中数学单学段 + 二次函数主线**"，**放弃"初高衔接"的跨学段表述**（它实为跨学段，需对齐两套课标条目体系，工作量翻倍）。高中部分只做**顶层的 1 个承接节点**（如"函数的单调性与最值"），不展开、不出题、不参与归因回溯，仅用于叙事。

> **卖点讲在嘴上，成本压在地上。**

---

## 十一、多模态能力设计

### 11.1 两条读图链路

**链路 A：单题读题**

```
上传图片 → 视觉模型识别题干、公式、图形 → 结构化
      → 知识点匹配（语义相似度 Top-3）→ 置信度判断
      → 高置信直接进入闭环 / 低置信追问澄清
```

**链路 B：试卷批量识别**（价值更高）

```
上传批改试卷 → 视觉模型切分题目 → 逐题识别题干与作答
      → 人工确认对错（双保险）→ 批量生成观测
      → 一次更新 10–20 个知识点的掌握度
```

链路 B 是"一次上传 = 一次全章诊断"的实现，也是 Demo 视频里最出效果的场景。

### 11.2 苏格拉底式引导策略

见第九章。核心是：**分层提示 + 退出通道 + 默认不给答案**。

### 11.3 模型调用的边界 ★v3 新增

**知微采用"代码编排为主、模型受约束执行为辅"的混合架构。模型从不拥有工具决策权。**

| 调用模式 | 谁在决策 | 典型场景 |
| --- | --- | --- |
| **确定性调用**（代码硬编排） | 后端代码 | 四路采集、BKT 更新、报告生成 |
| **模型结构化调用**（输出被枚举约束） | 大模型，但输出被严格约束 | 错误类型诊断、读题、讲解 |
| **流程内嵌调用** | 代码按规则分支 | `progress` 标记触发退出通道 |

**只有三处需要调用大模型**：

| 环节 | 输入 | 输出约束 |
| --- | --- | --- |
| 读题 / 试卷识别 | 图片 + 知识点列表 | 结构化 JSON，低置信降级追问 |
| 错误类型诊断 | 题目 + 作答 + `typical_errors` 列表 | **必须在枚举列表内选**，`confidence < 0.6` 不采纳 |
| 讲解与引导 | 掌握度 + 根因 + 错误类型 | 分步输出 + 自检，温度 0.2–0.3 |

**BKT 更新完全不含大模型**——它是纯数学计算。**这是刻意的**：掌握度这种要长期累积的状态，绝不能交给一个每次行为会漂移的模型去算。

**这个架构选择本身就是答辩得分点**：它正面回答了"大模型讲错题怎么办"（原文列为高风险）。答案不是"我们换个更好的模型"，而是"**我们不让模型决定关键结论**"。

### 11.4 幻觉抑制

大模型讲错一道题的代价远大于多讲对十道题。三重约束：

1. **知识点锚定**：讲解前先把题目绑定到具体知识图谱节点，讲解内容必须在该知识点定义范围内
2. **结构化输出 + 自检**：要求分步输出解题步骤，并增加一步自检
3. **低温度参数**：数学类讲解使用 0.2–0.3 温度，牺牲表达多样性换稳定性

### 11.5 优雅降级

识别失败时明确说"这道题我没看清，能麻烦你重拍一下吗"，并给出拍摄建议。**不要硬猜。** 一个坦诚的失败提示，比一个自信的错误讲解，在用户体验评分上的差距是决定性的。

---

## 十二、技术方案

### 12.1 架构分层

| 层 | 组成 | 职责 |
| --- | --- | --- |
| 交互层 | 学生端 Web、教师视角演示页 | 拍照、测评、路径展示、学习报告 |
| 智能层 | 证据融合引擎、错误诊断器、知识图谱引擎、多模态智能体、个性化推荐 | 加权 BKT、错误分类、归因、引导、路径规划 |
| 服务层 | 云函数 API 网关 | 模型代理、密钥托管、流式输出、鉴权 |
| 数据层 | 云数据库、云存储、大模型 + 视觉模型 | 空间、知识库、用户档案、答题记录、错误类型库 |

### 12.2 技术选型

| 模块 | 选型 | 理由 |
| --- | --- | --- |
| 前端 | React 18 + TypeScript | 生态成熟，组件化利于快速迭代 |
| 构建 | Vite | 冷启动快 |
| 样式 | TailwindCSS | 免写 CSS 文件，适合短期高强度开发 |
| 状态 | Zustand | 轻量，无样板代码 |
| 图谱可视化 | ECharts 力导向图 | 中文文档完善，性能好 |
| 后端 | 腾讯云 CloudBase 云函数（Node.js） | 零运维、免费额度足、赛事协办方 |
| 数据库 | CloudBase 文档数据库 | 与云函数同生态 |
| 云盘 | 云存储 + 文件元数据表 | 承载预置教材与用户文件 |
| 模型 | 文本模型 + 视觉模型 | 文本负责讲解与出题，视觉负责读题与试卷识别 |
| 部署 | EdgeOne Pages / CloudBase 静态托管 | 免费、自带 HTTPS、国内访问稳定 |
| 版本管理 | GitHub | 满足赛事源码仓库要求 |

### 12.3 数据结构 ★v3 新增 / v4 修订

**静态数据（随仓库走，人工维护，CI 校验）：**

```
data/knowledge/index.json              学科索引
data/knowledge/{subject}/{stage}.json  知识点数据
        └─ nodes[]  ← 图谱节点（含 source、typical_errors）
data/item_bank/{subject}/{stage}.json  题目库（★v4 补齐：≥220 题，train/retest 双池）
config/params.json                     算法参数（外置，见 12.6）
scripts/validate_data.py               静态数据校验（CI 必跑）
```

**运行时数据（云数据库，九张表）：**

| 表 | 职责 | 隔离键 |
| --- | --- | --- |
| `users` | 用户与认证 | — |
| `spaces` | **学习空间**（★v3 核心） | `space_id` |
| `mastery_profiles` | 掌握度画像 | `user_id + space_id + knowledge_point` |
| `evidence_events` | **四路统一证据入口**（v4 增 `mode` 字段） | `space_id` |
| `mastery_logs` | 掌握度更新日志（可复现） | `space_id` |
| `attributions` | 归因记录 | `space_id` |
| `dialogs` | 对话记录（含 `progress` 字段） | `space_id` |
| `item_bank` | 双题目池（静态种子写入，运行时只读） | `pool` |
| `recognitions` | **试卷识别任务（★v4 新增）**：识别结果落库，确认前刷新不丢 | `space_id` |

**核心表结构示例：**

**① 学习空间 `spaces`**

```json
{
  "space_id": "sp_001",
  "user_id": "u_1024",
  "name": "初中数学",
  "subject": "数学",
  "knowledge_source": ["kb_math_cz"],
  "is_default": true,
  "created_at": "2026-09-24T20:00:00Z"
}
```

> `knowledge_source` 存**数组**而非单值——当前一对多，将来升级为多选不需改表结构。

**② 掌握度画像 `mastery_profiles`**（跨会话状态，系统承重墙）

```json
{
  "user_id": "u_1024",
  "space_id": "sp_001",
  "knowledge_point": "math.cz.quadratic.vertex_form",
  "mastery": 0.62,
  "evidence_count": 7,
  "p_l0": 0.50,
  "status": "active",
  "last_updated": "2026-09-24T20:15:00Z",
  "last_evidence_type": "paper"
}
```

> `status` 取值 `active | blocked_by_prerequisite`，由 9.2 退出通道写入。

**③ 证据事件 `evidence_events`**（四路统一 schema，系统承重墙）

```json
{
  "event_id": "evt_8821",
  "user_id": "u_1024",
  "space_id": "sp_001",
  "knowledge_point": "math.cz.quadratic.vertex_form",
  "item_id": null,
  "source": "silent | paper | diagnose | self_report",
  "mode": "diagnose | baseline | retest | null",
  "result": "correct | wrong | unknown",
  "weight": 0.80,
  "alpha": null,
  "raw": {
    "image_url": "cos://...",
    "student_answer": "顶点(2,3)"
  },
  "dedup_key": "u_1024:sp_001:math.cz.quadratic.vertex_form:paper:491588",
  "created_at": "2026-09-24T20:10:00Z"
}
```

**四个缺一不可的字段：**

| 字段 | 作用 | 依据 |
| --- | --- | --- |
| `weight` / `alpha` | 决定 BKT 更新激进度 | 6.3 权重表 |
| `mode` | 区分诊断 / 基线 / 复测测量，**ΔAccuracy 分组统计的数据来源**（★v4 新增） | 8.4 指标一 |
| `dedup_key` | 实现"1 小时内同类证据去重合并"（kp 用**完整 id**；小时桶 = floor(unix/3600)） | 6.1 通路一 |
| `raw` | 保留原始证据，支撑归因可解释 | 8.2 "回溯路径可展示" |

**④ 掌握度日志 `mastery_logs`**

```json
{
  "log_id": "log_3391",
  "user_id": "u_1024",
  "space_id": "sp_001",
  "knowledge_point": "math.cz.quadratic.vertex_form",
  "before": 0.500,
  "p_obs": 0.818,
  "p_eff": 0.754,
  "after": 0.791,
  "weight": 0.80,
  "triggered_by": "evt_8821",
  "created_at": "2026-09-24T20:15:00Z"
}
```

> 附录 A 那段"同样答对，测评给 0.845、试卷给 0.791"的对比，**就是靠这张表算出来的**。答辩时能逐条调出。

**⑤ 归因记录 `attributions`**

```json
{
  "attribution_id": "attr_502",
  "user_id": "u_1024",
  "space_id": "sp_001",
  "from_kp": "math.cz.quadratic.extremum",
  "root_cause": "math.cz.quadratic.completing_square",
  "error_type": "prerequisite_gap",
  "path": ["math.cz.quadratic.extremum", "math.cz.quadratic.vertex_form", "math.cz.quadratic.completing_square"],
  "suspect_scores": { "math.cz.quadratic.vertex_form": 0.43, "math.cz.quadratic.completing_square": 0.29 },
  "verified": true,
  "verified_by": "evt_8825",
  "rejected_by_student": false,
  "created_at": "2026-09-24T20:20:00Z"
}
```

> `path` 与 `suspect_scores` 支撑 8.2 第四步的轨迹回放；`rejected_by_student` 对应 9.3 反驳入口。
> **v4 勘误**：v3 示例中 `suspect_scores: 0.72` 超出公式上界（`suspect = 0.6^d × (1 − mastery)`，d=1 时 ≤ 0.60，d=2 时 ≤ 0.36），已修正。本示例中 `vertex_form`（0.43）为最高嫌疑但验证未通过，排除后确认次高嫌疑 `completing_square`（0.29）——这正是"假设验证"机制价值的实证。**path / suspect_scores 的键一律使用完整 kp id**，与知识图谱直接 join。

**五个必须第一天就定死的设计决策（★v4 增补两项）：**

| 决策 | 结论 | 理由 |
| --- | --- | --- |
| **证据事件统一 schema** | 四路共用一张表 + `source` 字段区分 | 做成四张表则融合引擎需写四套逻辑，后期返工代价极高 |
| **掌握度只存当前值 + 日志** | 不存历史快照 | 8 天工期，快照会爆量且无用，日志足够回溯 |
| **题目池用 `pool` 字段隔离** | `pool: "train" \| "retest"` | 8.4 要求 ID 完全不重叠，字段过滤比两张表好维护 |
| **`space_id` 必须第一天到位** | 所有运行时表带 `space_id` | 若等引擎写完再加，需改引擎 + 所有接口 + 所有前端调用，代价翻倍 |
| **试卷识别结果必须落库** | `recognitions` 表 + 回显接口（★v4 新增） | 识别结果只存在内存中，确认前刷新即丢失——数据丢失类 bug 在 Demo 现场是灾难 |
| **判卷权收归服务端** | `answer` 绝不下发前端；一切对错由服务端判定（★v4 新增） | 前端判卷 = 答案必然泄漏；诊断模式的 `correct` 恒为 null，防学生反推答案污染测量 |

### 12.4 数据分层与通用性 ★v3 新增

**这是回答"如何既通用又聚焦"的核心：**

```
【第 1 层】引擎层（全局唯一，零隔离）
   BKT 引擎 / 归因引擎 / 错误分类器 / 引导策略
   ↑ 参数全局共用，不区分学科

【第 2 层】分类层（全局唯一，零隔离）
   五类错误枚举 / attribution_direction 映射表
   ↑ 数学和编程同样适用

【第 3 层】内容层（按学科隔离，成本 = 新建一个文件）
   data/knowledge/{subject}/{stage}.json
```

**判断标准**：

> **通用 = 同一套引擎 + 同一套分类 + 不同内容文件。**
> **不通用 = 同一套代码里 `if (subject === 'math')`。**

**如果发现自己在写 `if (subject === 'math')` 分支，那就是走错了方向。** 当前方案中不存在任何此类分支——**架构本来就是通用的，只是扁平的数据让这个事实不够明显。**

**所谓"隔离麻烦"，根源是把"引擎参数"和"学科内容"当成了同一种东西。** 实际上方案中六个参数全部学科无关，五个错误类型也全部学科通用；真正需要隔离的只有知识点 JSON 文件本身。

### 12.5 关键接口

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/api/auth/register` `/api/auth/login` | POST | **极简认证**（见 12.6），注册即自动创建默认空间 |
| `/api/space/list` `/api/space/create` | GET / POST | 学习空间：列表与新建（含同学科冲突检测） |
| `/api/space/{id}/drive` | GET | 空间关联的云盘文件（P1） |
| `/api/evidence/self-report` | POST | 通路四：写入自报先验（不作观测） |
| `/api/evidence/paper` | POST | 通路二：上传试卷，识别结果写入 `recognitions` |
| `/api/evidence/paper/{recognition_id}` | GET | ★v4 新增：识别结果回显（确认前刷新不丢） |
| `/api/evidence/paper/confirm` | POST | 通路二：人工确认对错后批量写入观测 |
| `/api/diagnose/next` | POST | 通路三：返回下一道自适应诊断题（题池由服务端按 mode 推导） |
| `/api/diagnose/submit` | POST | 提交答案，**服务端判卷**，加权更新掌握度；`correct` 仅测量模式返回 |
| `/api/error/classify` | POST | 错误类型诊断，返回结构化判定（低置信退回追问） |
| `/api/attribution/analyze` | POST | 归因分析（含错误类型分流与先修回溯） |
| `/api/attribution/{attribution_id}` | GET | ★v4 新增：归因结果回显（结果页刷新用） |
| `/api/attribution/verify` | POST | 验证题作答，**服务端判卷**，答错排除候选取次高 |
| `/api/plan/generate` | POST | 生成补救路径与题序列 |
| `/api/agent/chat` | POST (SSE) | 智能体对话，流式返回，含结构化 `progress`；传图读题匹配置信度 ≥ 0.6 时**内部产生弱负证据**（无独立静默采集接口） |
| `/api/agent/reject` | POST | 学生反驳归因结论 |
| `/api/report/summary` | GET | 学习报告数据（ΔAccuracy 按 `mode` 分组统计） |

> 共 **19 个接口**，完整请求/响应签名与错误码见施工文档 `API_CONTRACT.md`（已冻结，v1.1）。

### 12.6 安全与后端严谨度取舍 ★v3 新增

**认证：做，但只做最薄的版本。**

| 为什么必须做 | 具体后果 |
| --- | --- |
| 掌握度是**有状态**的 | 没有账号，掌握度存哪？换设备归零，产品前提崩塌 |
| 效果验证需要**可追溯样本** | 5 名起步（扩至 8–10）学生各走一次闭环，无身份无法对应"这几例是谁的" |
| 教师审阅需要**干净的案例** | 教师看到的是"一个学生的两次复测记录"，混在一起审不了 |

**最小方案（约半天）：**

```
手机号（或邮箱）+ 密码 → 服务端签发长期 Token → 前端存 localStorage
        → 所有 /api/* 请求带 Token → 云函数校验
```

**Token 机制（★v4 明确）**：无状态签名 `token = HMAC(user_id, SERVER_SECRET)`，**不落库**；`SERVER_SECRET` 放云函数环境变量。校验 = 解出 user_id + 验签，无需查库。

**明确不做**：短信验证码、邮箱验证、密码找回、三方登录、JWT 刷新机制。**评委会问的问题里没有一个是关于认证的。**

**关键：`user_id` 与 `space_id` 必须从第一天贯穿全链路。**

**后端严谨度：两刀切。**

|  | 线 A：算法与状态（BKT、归因、采集） | 线 B：模型调用与流式对话 |
| --- | --- | --- |
| 严谨度 | **高** | **中低** |
| 理由 | 掌握度跨会话累积，算错会污染画像且**错误会一直滚下去** | 对话是瞬时的，这次讲不好下次可重来 |
| 关键要求 | 参数可配、更新可复现、留 delta 日志 | 不崩、流式、能降级 |
| 时间占比 | **60%** | **30%** |

**线 A 三条硬要求：**

1. **BKT 更新必须幂等且可复现**——同一证据重复提交不重复扣分（`dedup_key` 即为此）
2. **每次更新留 delta 日志**——记录 `P(L)` 变化与触发证据（`mastery_logs`），答辩时即"可解释"的实证
3. **参数外置**——`w`、`α`、`P(S)`、`P(G)`、`P(T)` 全部放配置，不硬编码。9/24 调参时需一秒改完

**线 B 三条不可破的底线：**

| 底线 | 原因 |
| --- | --- |
| **API Key 绝不落前端**，强制走云函数代理 | 唯一不能妥协的安全项 |
| **超时与降级** | 模型挂了要能返回"这道题我没看清，麻烦你重拍一下" |
| **SSE 断了要能续** | Demo 现场网络异常时，退化为非流式返回也比白屏强 |

**明确可以"不严谨"的地方（省时间）：**

| 项目 | 结论 | 理由 |
| --- | --- | --- |
| 并发控制 | 不做 | 8 个学生不可能并发 |
| 数据备份 | 不自己写 | 云数据库自带 |
| 接口限流 | 只做最简单的 | 防止 demo 手抖连点 |
| 错误码规范 | 统一 `{code, msg}` 即可 | 不设计复杂错误体系 |
| 数据库索引 | 只给 `user_id` / `space_id` 加 | 其他数据量小 |
| 单元测试 | **只测 BKT 一个模块** | 它算错最致命，其他靠手动跑 |

**其他安全项：**

- 图片上传走云存储，不直接暴露模型接口
- 用户数据最小化，只存学习相关必要字段
- **试卷图片设保留期限**——`evidence_events` 加 `expire_at` 字段，支持用户主动删除。**这一条若 9/20 未设计进去会变成结构性返工**，且直接对应"用户数据合规"自查项

---

## 十三、功能清单

### P0（必须完成，缺一不可 · v4 收敛后共 11 项）

| 功能 | 说明 |
| --- | --- |
| **学习空间** | **默认自动创建 + 绑定知识库 + 同学科冲突检测** |
| 极简认证 | 注册登录、token 留存（注册即自动建空间） |
| 三路采集入口 | 自报问卷、测评、试卷上传全部可用；静默采集并入对话传图（无独立入口） |
| 加权 BKT 引擎 | 证据权重融合的三段式更新跑通；通过 3 个自检用例（0.845 / 0.791 / 0.244） |
| 错误类型诊断 | 五类错误分类可运行；低置信退回追问 |
| 归因定位 | 错误类型分流 + 先修回溯 + 假设验证 |
| 知识图谱可视化 | 掌握度着色 + 归因路径高亮（兼做学习路径页） |
| 智能体对话 | 苏格拉底式引导，流式输出，含退出通道；传图读题进入闭环 |
| 新题复测闭环 | 题目池零重叠（脚本校验），输出正确率变化 |
| 学习报告页 | 掌握度分布、缺口清单、ΔAccuracy（按 mode 分组统计） |
| **题库与错误库数据资产** | 20 知识点配满 typical_errors + ≥220 题双池（**数据本身即 P0 交付物**） |

### P1（9/23 闭环跑通后才允许碰）

| 功能 | 说明 |
| --- | --- |
| 试卷识别体验打磨 | 切分准确性、识别耗时优化 |
| 归因反驳完整流 | 反驳按钮 P0 已有；"追加再验证题"闭环 P1 |
| 云盘页面 | 展示预置教材与用户文件 |
| 自定义知识库占位 | 禁用态按钮（见 4.5），不建假文件 |

### P2（v4 起明确不做，理由如下）

| 砍掉项 | 理由 |
| --- | --- |
| 教师视角演示页（真实页面） | 改为 PPT 静态设计图，省 1.5 天 |
| 第二学科骨架切片 | "引擎无 `if (subject)` 分支"本身就是可扩展性证明，半成品数据反而减分 |
| 学习报告导出 PDF | 不贡献闭环 |
| 语音提问 | 不贡献闭环 |
| 错题本归档 | 归因记录与证据事件已含等价信息 |

---

## 十四、界面与交互设计

### 14.1 页面清单

1. **首页 / 登录页**——一句话说清产品是什么
2. **空间列表页**——默认已有空间，新建为次要按钮
3. **自报问卷页**——5 个选项，10 秒完成（兼问"想补哪一块"以自动建空间）
4. **测评页**——单题呈现，进度可视化，无对错反馈
5. **试卷上传页**——识别进度 + 对错人工确认
6. **归因结果页**——★核心页面，回溯路径 + 根因 + 错误类型 + 反驳按钮
7. **知识图谱页**——掌握度着色 + 路径高亮 + 待完成节点（兼做学习路径页，v4 合并）
8. **对话辅导页**——智能体引导，支持上传图片（含传图读题与退出提示）
9. **学习报告页**——掌握度分布、缺口清单、新题正确率变化
10. **云盘页**——预置教材与用户文件（P1）

> v4 删除"教师视角演示页"（改为 PPT 静态设计图）；"学习路径页"并入知识图谱页。共 10 页。

### 14.2 设计原则

- **一屏一件事**：每个页面只承载一个核心任务
- **掌握度用颜色说话**：颜色语义全局一致，必须提供图例
- **不制造焦虑**：待巩固用中性暖色而非刺眼红色；措辞用"这一环还可以再稳一点"
- **采集永不打扰**：静默采集绝不用弹窗打断用户
- **第一屏就让用户开始**，不解释产品
- **空间不占首屏**：默认自动创建，避免"先建空间"造成流失

### 14.3 视觉基调

- **主色**：低饱和青蓝——安静、专注，匹配"陪着你学"的气质。避开高饱和橙红（应试焦虑的颜色）
- **辅助色**：暖橙（待巩固）、青绿（已掌握）
- **字体**：正文无衬线，中文用系统字体栈，避免网络字体加载抖动
- **图形**：知识图谱是全篇视觉主角，留足空间

### 14.4 界面语气

原则：**像一位耐心的学长，不像一个评判者。**

| 场景 | 不说 | 说 |
| --- | --- | --- |
| 知识点薄弱 | 你掌握很差 | 这一环还有点晃，我们再稳一下 |
| 答错了 | 回答错误 | 这个坑很常见，我们看看它是怎么来的 |
| 归因成功 | 检测到你的知识缺陷 | 找到啦——真正卡住你的是这里 |
| 触发退出通道 | 你答不上来，换一题 | 我们先往回看一眼 XX，那里可能是关键 |
| 全部通过 | 全部正确 | 这几处你已经很扎实了，我们换个地方看看 |
| **新建同学科空间** | 已存在同名空间 | 你已有一个初中数学空间，我们直接去那里吧 |

---

## 十五、开发排期（v4：甲乙双线 · AI 主导开发 · 两人各自全栈）

**分工原则：按业务纵切，不拆前后端**——甲（诊断线）端到端负责数据与算法，乙（教学线）端到端负责交互与交付。两人都是全栈，代码主力由 AI（LearnBuddy）生成，人力压在**数据校验、真实学生、答辩材料**三件 AI 干不了的事上。**接口与字段以冻结的 `API_CONTRACT.md` 为准**（给 AI 的"宪法"，贴契约生成代码，字段天然对齐）。

| 日期 | 甲（诊断线） | 乙（教学线） | 当日必交 |
| --- | --- | --- | --- |
| 9/18 | 需求冻结、方案定稿、建仓库 | 同左 | 本方案 v3 + GitHub 仓库（已完成） |
| 9/19 | 表结构草案 + **招 3 名学生** | 脚手架 + 部署通路 | **契约冻结 ✅（已完成，19 接口/九表，v1.1）** + 学生名单 |
| 9/20 | 图谱 20 节点 + 典型错误库初稿 | 空间页 + 认证 | 能登录、有默认空间 |
| 9/21 | **题库 ≥220 题（逐题人工验算）** + BKT 引擎 + 单测 | 测评页 + 试卷确认页 | 自报 → 测评全链路 |
| 9/22 | 错误诊断 + 归因可演示 | 对话 + SSE + 退出通道 | 归因演示 |
| 9/23 | 归因联调 + 复测闭环 | 图谱页 + 报告页 | **完整闭环跑通** |
| 9/24 | **功能冻结**；5 名学生实测记录 | 演示打磨 + Demo 草稿 | 公网链接 + 实测数据 |
| 9/25 | 教师审阅 + 数据成表 | Demo 视频 + PPT | 三项材料齐备 |
| 9/26 | 对练答辩 + **上午提交** | 同左 | 提交完毕 |

### 排期纪律（八条）

1. **9/24 功能冻结**，之后一行新功能都不加
2. **每天必须有一个可演示的版本**，宁可功能少，不能链路断
3. **MVP 底线**：1 个章节 + 20 个知识点（错误库配满）+ ≥220 题双池 + 1 条能完整走通的闭环 + 5 名真实学生数据
4. **9/25 晚上必须完成提交材料**，留一天纯缓冲
5. **`space_id` 与 `user_id` 必须 9/19 就贯穿所有表**——越晚越贵（契约已冻结，照办即可）
6. **9/20 之前必须把 `typical_errors` 填满**——它是唯一无法自动化的资产，是整条链路的燃料
7. **9/21 题库逐题人工验算**——AI 生成数学题的错误率不低且错得隐蔽，**每题必须算一遍**，错题直接删
8. **每完成一个模块立刻 commit**（含"推翻重来"记录）——提交历史本身是"AI 工具使用"25 分的评分材料，禁止一次性大提交

---

## 十六、效果验证方案

答辩时最容易被问的一句话是："**你怎么证明你的归因是对的？**" 这一章就是答案。

### 16.1 指标一：新题正确率提升（客观、与模型无关）

| 项目 | 设计 |
| --- | --- |
| 基线测量 | 补救前，用 **3 道**复测池新题测该知识点正确率（mode=baseline） |
| 干预 | 执行完整的诊断 → 归因 → 处方流程 |
| 复测 | 补救后，用**另外 3 道**复测池新题再测（mode=retest，与基线题不重复） |
| 关键约束 | 复测池与训练池**题目 ID 零重叠**，由校验脚本断言（retest 池配额 ≥6/知识点即为此设计） |
| 指标 | `ΔAccuracy = 复测正确率 − 基线正确率`（按 `evidence_events.mode` 分组统计） |
| 判据 | `ΔAccuracy ≥ 0.3` 且复测正确率 ≥ 0.75 |

**汇报话术**：「复测新题正确率从 33% 提升到 80%」。这句话里"新题"三个字是关键——它证明学生是真的会了，不是记住了答案。

### 16.2 指标二：归因认可率（证明算法是真的对）

| 项目 | 设计 |
| --- | --- |
| 样本 | **5 名真实学生起步**（招募顺利扩至 8–10），各完成一次完整闭环（**全部落在同一空间、同一章节**），累计 **≥15 条归因记录** |
| 采集 | 记录系统输出的归因结论（根因知识点 + 错误类型） |
| 判定 | 请 1–2 位一线教师人工审阅，逐例判定"合理 / 部分合理 / 不合理" |
| 指标 | 教师认可率 = （合理 + 部分合理）/ 总例数 |
| 目标 | **≥ 75%（冲刺 80%）**——5 名样本下按 ≥15 条记录计，比 8–10 名全量更早可测、更诚实 |

**汇报话术**：「15 条归因记录中，一线教师认可 12 条」。**v4 提醒：一律使用实测值**，规模小但真实，远好于编造的大数字。

**样本同质是关键。** 历史经验表明，样本散落在多学科多学段时，教师审阅需切换多套知识体系，认可率必然下降。**集中在一个章节，全部归因记录都落在二次函数上，教师一眼就能判断对错。**

这个实验成本很低（一天），但它同时喂饱两项评分：**技术创新性**（证明算法可行）和**工程完整性**（证明系统真的跑通了）。而且它正好是教育学背景队友的实质产出。

### 16.3 指标三：引导有效性（展望）

对比实验设计：A 组使用"苏格拉底式引导"，B 组直接给答案，比较两组在**一周后新题**上的表现。

这个实验设计最硬核（能证明"引导比给答案更有效"这一核心主张），但需要更长周期与更多样本。建议在 PPT 的"后续规划"页作为展望提出，展示团队的研究意识。

### 16.4 执行清单

- [x] 建立双题目池契约（train≥5 / retest≥6 每知识点，≥220 题；零重叠由 `validate_data.py` 断言）
- [ ] 招募 5 名真实学生起步（最好覆盖不同水平，**同一章节**）——**9/19 就开始，拖到 9/24 一定招不满**
- [ ] 准备教师审阅表单（归因结论 + 判定选项）
- [ ] 记录原始数据，形成可展示的数据表格
- [ ] 整理成 PPT 中的一页"效果验证"（**全部使用实测值**）

---

## 十七、评审维度对标

| 评分维度 | 分值 | 我们的得分点 |
| --- | --- | --- |
| 技术创新性 | 30 | **学习空间结构**；四路采集与证据权重融合；贝叶斯知识追踪构建知识点级掌握度；**错误类型诊断驱动的归因算法**（含前置分流与假设验证）；拓扑剪枝冷启动；引导退出通道（教学反馈接回诊断闭环） |
| 工程完整性 | 25 | 四层清晰架构；前后端分离；云函数代理保障密钥安全；知识点数据与代码解耦 + **课标逐条溯源**；**题目库双池隔离 + CI 校验脚本**；**统一证据 schema + 九张表 + 19 接口契约冻结**；P0 功能全部可运行；GitHub 完整提交历史 |
| AI 工具使用 | 25 | LearnBuddy 全流程参与：**契约驱动开发**（四份施工文档冻结后指挥 AI 生成）、知识图谱与题目库批量生成、算法实现、文档撰写、测试用例设计，全程留痕并含"推翻重来"的过程记录（含**AI 生成契约后自审发现 13 处缺陷并修订至 v1.1** 的实录） |
| 用户体验 | 20 | 采集永不打扰；**空间默认自动创建、不占首屏**；试卷上传"识别 + 人工确认"双保险且**识别结果落库不丢**；归因路径可视化；流式响应；识别失败优雅降级；归因结论可反驳；触发退出通道时的坦诚措辞 |
| 加分项 | +3 | 跨专业协作 +2（⚠️ 两人团队需按赛事规则原文核对加分资格，不计入必得预算）；社媒传播 +1 |

---

## 十八、两人团队的分工与教育学支撑（v4 按实际人数修订）

**实际配置：2 人**。v3 的"计算机 1–2 人 + 教育学 1 人"按当前人数不可实现，v4 改为**两人各自全栈 + 纵切分工**：

| 角色 | 职责线 | 承担模块 |
| --- | --- | --- |
| **成员甲 · 诊断线** | 数据与算法 | 知识图谱 / 典型错误库 / 题目库校验、BKT 引擎、错误诊断、归因算法、测评与试卷接口、学生实验执行 |
| **成员乙 · 教学线** | 交互与交付 | 脚手架 / 认证 / 部署、测评页 / 试卷确认页 / 空间页、对话 + SSE + 退出通道、归因结果页 / 图谱页 / 报告页、Demo 视频 + PPT |

**为什么纵切而非前后端拆分**：AI 主导开发时，AI 擅长横向铺完整链路，人为切前后端只会制造接口对齐成本；纵切让任何一人卡住时另一人不被阻塞，责任边界清晰。唯一需要对齐的是**冻结的 API 契约**。

**教育学支撑的处理（两人无教育学成员时的诚实方案）**：

- **理论依据保留为设计依据**（答辩可讲）：掌握学习理论（Mastery Learning）、认知诊断的属性层级方法（AHM）、错误分析理论（Error Analysis）、支架式教学与最近发展区（ZPD）——这些是方案的理论底座，与人数无关
- **`typical_errors` 库**：由**更懂教学的一方主笔**（两条线里必然有一人更合适），并**请一线教师审阅**（与 16.2 的教师审阅为同一批人，一鱼两吃）
- **跨专业加分（+2）**：按赛事规则原文核对两人专业配置是否满足；**不满足就不硬凑**，该分不进预算

**PPT 呈现**：专门一页讲"教育理论 → 技术实现"的映射。这一页会把作品从"一个不错的工具"提升到"一个有理论支撑的教学系统"——**它不依赖团队里是否真有教育学成员**。

---

## 十九、风险与应对

| 风险 | 影响 | 应对方案 |
| --- | --- | --- |
| 冷启动数据不足 | 高 | 四路漏斗 + 自报先验 + 拓扑剪枝 |
| 归因过度发散误导学生 | 高 | 错误类型前置分流，非知识性错误不进入归因 |
| 效果验证缺乏说服力 | 高 | 新题正确率 + 教师认可率双指标（一律实测值） |
| 大模型讲解出错 | 高 | **模型只做受约束结构化输出**；知识点锚定 + 自检 + 低温 |
| **AI 生成题目含隐蔽数学错误（v4 新增）** | **高** | **逐题人工验算（9/21 甲全天）+ `distractors` 绑定典型错误 + 校验脚本断言配额** |
| **范围蔓延 / 临时加功能（v4 新增）** | **高** | **PRD"不做"清单 + 契约冻结（改契约需双人同意）+ 9/24 功能冻结** |
| **`progress` 标记误判触发退出** | 高 | 结构化字段 + 判断依据 + 下一题反向校验 |
| **多空间同学科导致数据分裂** | 高 | 空间名固定为知识库名 + 新建时冲突检测 |
| **知识点权威性被质疑** | 高 | 点名三部课标 + 节点级 `source` 溯源 |
| 手写与批改符号识别失败 | 中高 | "识别 + 人工确认"双保险，不硬猜；**识别结果落库（v4），刷新不丢** |
| 8 天工期不足 | 高 | v4 范围收敛（P0 共 11 项）+ 甲乙双线纵切 + AI 主导生成 + 每日可演示版本 |
| **自定义知识库引发空间失效** | 高 | 仅做禁用态占位，不开放真实上传 |
| 前端泄露 API Key | 中高 | 所有模型调用强制走云函数代理 |
| 版权风险 | 高 | 知识点框架基于公开课标并逐条溯源；题目模型生成 + 人工校验；开源库在文档中声明 |
| 采集打扰用户导致流失 | 中 | 静默采集永不弹窗；同类证据时间窗口去重 |
| 现场演示网络异常 | 中 | 录制备份演示视频；关键页面本地缓存 |
| 团队协作冲突 | 中 | **纵切分工边界清晰；接口争议以契约为准；每日 30 分钟同步** |
| **关键人单点故障（v4 新增，两人团队特有）** | 中 | 每日 commit 保持仓库共享；关键算法文档化（ALGORITHM.md 可直接交给 AI 重写实现） |

---

## 二十、知识产权与合规自查

| 检查项 | 状态 | 说明 |
| --- | --- | --- |
| 作品原创性 | 待执行 | 全程自主开发，不搬运他人作品 |
| **知识点来源可溯源** | **待执行（v3 强化）** | **逐节点 `source` 字段映射至《义务教育数学课程标准（2022年版）》内容条目；先修关系标注为推导** |
| 不使用他人题库 / 教辅内容 | 待执行 | 知识点框架来自公开课程标准；题目模型生成后人工校验 |
| 云盘预置教材的版权 | **待执行（v3 新增）** | **预置教材仅作对照参考资料，需确认公开展示的合规性；必要时改为课标文件 + 授权教材目录截图** |
| 开源库声明 | 待执行 | README 与 PPT 中列出所用开源框架及许可证 |
| 不提交纯 AI 生成未验证方案 | 待执行 | 所有算法与代码均经实际运行验证；**≥220 道 AI 生成题目逐题人工验算**；BKT 通过 3 个自检用例单测 |
| 内容合规 | 待执行 | 无违法违规内容 |
| 一稿一赛 | 待执行 | 本作品仅参加本赛事本赛道 |
| 用户数据合规 | 待执行 | 采集前明确告知；试卷图片设保留期限（`expire_at`）可删除；不采集无关信息 |
| 作品用于宣传授权 | 已知悉 | 组委会可标注作者信息后用于赛事宣传 |

---

## 二十一、提交材料清单

| 材料 | 要求 | 状态 |
| --- | --- | --- |
| 作品链接 | 可直接在线体验 | 待完成（9/24） |
| Demo 视频 | 3 分钟内，完整展示核心功能 | 待完成（9/25） |
| 作品介绍 PPT | 简介、赛题理解、技术方案、核心功能、应用价值 | 待完成（9/25） |
| 源代码仓库 | GitHub / Gitee | 待建立（9/18） |
| LearnBuddy 使用记录 | 展示 AI 辅助开发过程 | 持续积累中 |
| 验证数据 | 新题正确率 + 教师认可率 | 待完成（9/25） |
| 跨学科说明 | 团队跨专业协作说明 | 加分项 |
| 社媒传播截图 | 传播记录 | 加分项 |

---

## 二十二、品牌设定

### 22.1 名字释义

**知微**，取自成语"见微知著"——见到细微的苗头，就能预知事物的实质与发展。

这个名字精准对应产品的核心能力：**从一道错题（微）出发，推导出整个知识结构中的真实缺口（著）。**

### 22.2 Slogan 候选

- **知微 · 会追问的 AI 学习伴侣**（推荐，态度最清晰）
- 知微 · 从一道错题，看见整个知识结构
- 知微 · 不给你答案，给你方法
- 知微 · 只补你真正不会的那一环

**建议组合**：产品名「知微」+ 作品副标题「基于知识图谱归因的一对一定制 AI 学习伴侣」。

### 22.3 视觉基调

- **主视觉**：一张发光的知识网络，其中一个节点被点亮并延伸出一条路径——象征"从一点看见全局"
- **主色**：低饱和青蓝（安静、专注、陪伴感）
- **辅助色**：暖橙（待巩固）、青绿（已掌握）
- **字体气质**：清爽无衬线，字重克制

---

## 附录 A · 知识点与题目数据示例（含来源、典型错误、题目双池）

```json
{
  "meta": {
    "subject": "数学",
    "stage": "初中",
    "version": "3.0",
    "primary_standard": "义务教育数学课程标准（2022年版）"
  },
  "nodes": [
    {
      "id": "math.cz.quadratic.completing_square",
      "name": "配方法",
      "chapter": "一元二次方程",
      "difficulty": 2,
      "source": {
        "standard": "义务教育数学课程标准（2022年版）",
        "item": "第三学段·数与代数·方程与不等式",
        "type": "direct"
      },
      "prerequisites": ["math.cz.algebra.identity"],
      "prerequisite_basis": "教材章节顺序 + 学科逻辑推导",
      "successors": ["math.cz.quadratic.vertex_form"],
      "typical_errors": [
        {
          "code": "const_sign",
          "desc": "移项后常数项符号处理错误",
          "error_type": "procedural_slip",
          "remedy": "强调'等式两边同时加减'，要求写出每一步"
        }
      ]
    },
    {
      "id": "math.cz.quadratic.vertex_form",
      "name": "二次函数的顶点式",
      "chapter": "二次函数",
      "difficulty": 3,
      "source": {
        "standard": "义务教育数学课程标准（2022年版）",
        "item": "第三学段·数与代数·函数·二次函数",
        "type": "derived"
      },
      "prerequisites": [
        "math.cz.quadratic.completing_square",
        "math.cz.function.graph"
      ],
      "prerequisite_basis": "教材章节顺序 + 学科逻辑推导",
      "successors": ["math.cz.quadratic.extremum"],
      "typical_errors": [
        {
          "code": "sign_confusion",
          "desc": "顶点坐标 h 的符号混淆",
          "error_type": "concept_confusion",
          "remedy": "用 y=(x−h)²+k 与 y=(x+2)²+3 对照，强化括号内取反规则"
        },
        {
          "code": "no_vertex_form",
          "desc": "不会用配方法把一般式化为顶点式",
          "error_type": "method_gap",
          "remedy": "演示配方法三步：提系数、配一次项一半的平方、还原"
        }
      ]
    },
    {
      "id": "math.cz.quadratic.extremum",
      "name": "二次函数的最值",
      "chapter": "二次函数",
      "difficulty": 3,
      "source": {
        "standard": "义务教育数学课程标准（2022年版）",
        "item": "第三学段·数与代数·函数·二次函数",
        "type": "direct"
      },
      "prerequisites": ["math.cz.quadratic.vertex_form"],
      "prerequisite_basis": "教材章节顺序 + 学科逻辑推导",
      "successors": [],
      "typical_errors": [
        {
          "code": "ignore_domain",
          "desc": "忽略定义域限制直接套顶点",
          "error_type": "misreading",
          "remedy": "训练审题：先划出定义域再决定用哪一步"
        },
        {
          "code": "opening_direction",
          "desc": "开口方向判断错误导致取到反向极值",
          "error_type": "concept_confusion",
          "remedy": "对照 a>0 与 a<0 两组图像讲解"
        }
      ]
    }
  ]
}
```

### 题目库数据示例 ★v4 新增（v3 缺口补齐）

> 题目是整个闭环的"子弹"：诊断、验证、巩固、基线、复测五处都要出题。v3 只定义了 `item_bank` 表名而没有字段契约，v4 补齐。**配额：每知识点 train ≥5 + retest ≥6，20 节点共 ≥220 题**；节点 `sample_items` 引用题目 ID。

```json
{
  "item_id": "q_cz_vertex_003",
  "knowledge_point": "math.cz.quadratic.vertex_form",
  "pool": "retest",
  "type": "fill",
  "difficulty": 3,
  "stem": "抛物线 y = (x + 2)² + 3 的顶点坐标是____。",
  "options": null,
  "answer": "(-2, 3)",
  "solution_steps": [
    "顶点式 y=(x−h)²+k 中顶点为 (h,k)",
    "y=(x+2)²+3 即 y=(x−(−2))²+3",
    "故 h=−2, k=3，顶点 (−2, 3)"
  ],
  "distractors": [
    {
      "answer": "(2, 3)",
      "typical_error_code": "sign_confusion",
      "explanation": "未对括号内取反，h 符号错误"
    }
  ]
}
```

**`distractors` 的价值（v4 关键设计）**：把学生的典型错误答案与 `typical_errors` 的 code 绑定——错误诊断时学生作答可直接匹配典型错误，置信度显著提升；它同时就是"归因认可率"实验的素材库。

**双池隔离由校验脚本保证**：`validate_data.py` 断言 item_id 全局唯一（即双池天然零重叠）、配额达标、`distractors.typical_error_code` 必须存在于该知识点 `typical_errors`——CI 必跑，不靠人肉核验。

### 掌握度更新示例（单源，w = 1.0）

「顶点式」当前 `P(L) = 0.5`，参数 `P(S)=0.1`、`P(G)=0.2`、`P(T)=0.15`：

**学生答对：**

```
P_obs = 0.5 × 0.9 / (0.5 × 0.9 + 0.5 × 0.2) = 0.45 / 0.55 = 0.818
P_eff = 1.0 × 0.818 + 0 × 0.5 = 0.818
P_new = 0.818 + (1 − 0.818) × 0.15 = 0.845
```

**学生答错：**

```
P_obs = 0.5 × 0.1 / (0.5 × 0.1 + 0.5 × 0.8) = 0.05 / 0.45 = 0.111
P_eff = 1.0 × 0.111 + 0 × 0.5 = 0.111
P_new = 0.111 + (1 − 0.111) × 0.15 = 0.244
```

### 掌握度更新示例（多源，w = 0.8）

同样答对，但证据来自**批改试卷**（`w = 0.80`）：

```
P_obs = 0.818
P_eff = 0.80 × 0.818 + 0.20 × 0.5 = 0.754
P_new = 0.754 + (1 − 0.754) × 0.15 = 0.791
```

对比可见：**同样答对一题，来自测评的更新（0.845）比来自试卷识别的更新（0.791）更激进**——因为我们对试卷识别的结果保留了一分怀疑。**证据权重的意义就在这里。**

---

## 附录 B · 错误类型诊断的结构化设计

**输入**：题目文本 + 学生作答内容（文本或图片）+ 匹配到的知识点及其 `typical_errors` 列表

**输出 schema**：

```json
{
  "knowledge_point": "string, 知识点 ID",
  "error_type": "prerequisite_gap | concept_confusion | method_gap | procedural_slip | misreading",
  "matched_typical_error": "string, 匹配到的典型错误 code，未匹配则为 null",
  "confidence": "number 0-1",
  "evidence": "string, 判定依据，一句话",
  "attribution_direction": "upstream | self | none"
}
```

**`attribution_direction` 的映射规则**（可直接在代码中实现）：

| error_type | attribution_direction | 后续动作 |
| --- | --- | --- |
| prerequisite_gap | upstream | 触发先修回溯 |
| concept_confusion | self | 根因即本节点，直接处方 |
| method_gap | self | 根因即本节点，直接处方 |
| procedural_slip | none | 转熟练度训练，不进归因 |
| misreading | none | 转审题训练，不进归因 |

**提示词设计要点**：

1. 先让模型复述"学生具体错在哪一步"，再判定类型——强制它先观察后判断，显著降低误分类
2. `confidence < 0.6` 时不采纳该判定，退回向学生追问澄清
3. `error_type` 从**五值全局枚举**中选；`matched_typical_error` 从该知识点 `typical_errors` 的 code 中选（无匹配为 null），**不允许模型自由发明**（保证可枚举、可统计）——v4 澄清：两者的约束来源不同
4. `attribution_direction` 由服务端映射表硬编码输出，**不采信模型对该字段的判断**

**v3 新增——对话引导的结构化输出 schema：**

```json
{
  "reply": "string, 面向学生的自然语言回复",
  "progress": "boolean, 本轮学生的回答是否触及正确方向",
  "progress_reason": "string, 一句话判断依据",
  "next_action": "continue | hint_down | give_solution | exit_channel"
}
```

> `progress` 由代码读取结构化字段判定，**不解析自然语言**——避免模型在自由文本中自说自话。同时用学生下一题的作答表现反向校验其准确性。

---

## 附录 C · LearnBuddy 使用记录留痕建议

「AI 工具使用」占 25 分，且要求提交历史对话记录。建议有意识地留下这些痕迹：

**按工作类型分类：**

1. **知识图谱批量生成**——让 LearnBuddy 按课程标准批量生成知识点与先修关系草案，再人工修正。记录"生成了多少条、人工改了多少条"。
2. **错误类型库构建**——收集各知识点的典型错误，让 AI 归类到五类错误。这是最能体现 AI 协作深度的环节。
3. **算法调优**——BKT 参数调整、证据权重标定、归因跳数上限讨论。
4. **原型开发**——页面骨架生成、组件拆分、样式调整。
5. **问题排查**——报错信息、定位过程、修复方案。真实的 debug 记录比顺利的过程更有说服力。
6. **文档撰写**——本方案、PPT 文案、Demo 脚本的迭代。
7. **测试用例设计**——让 AI 找边界情况。

**呈现技巧**：不要只截图成功结果，**保留一次"推翻重来"的过程**。评审看几十份材料，"AI 帮我生成了代码"千篇一律；而"我们最初让错题一律往上游回溯，发现过度归因严重，于是引入错误类型前置分流"这样的记录，才是深度使用的真凭实据。

**v3 建议补充的两处留痕**：

- **学习空间结构的演进**——"最初我们把多学科数据平铺在一张画像上，发现学生学两门课时数据混在一起，于是引入学习空间作为隔离边界"
- **数据契约的修正**——"最初四路采集各写了一套数据结构，融合引擎代码膨胀四倍，于是统一为一张证据表 + `source` 字段区分"

**v4 建议补充的三处留痕（本次实际发生，直接可用）**：

- **契约驱动开发**——"我们先冻结四份施工文档（API_CONTRACT / PRD / ALGORITHM / DATA_SCHEMA），再让 AI 按契约生成代码——AI 给契约就能干活，没有契约每次给的都不一样"
- **AI 生成契约后的自审修订**——"AI 生成契约 v1 后，隔一轮冷眼自审发现 13 处缺陷（6 处会直接导致联调事故：识别结果无处存放、验证接口判卷死锁、ΔAccuracy 无法计算、对错泄漏、参数冲突、配额算术错误），修订至 v1.1 并全量变更留痕"——**这是比"推翻重来"更高级的深度使用证据**
- **勘误实录**——"v3 方案里归因示例的嫌疑分 0.72 超出公式上界（d=2 时 ≤0.36），被 v4 评审发现并修正——我们自己抓出了自己文档里的数学错误"

**量化呈现更有力**："AI 协助生成 320 条知识点、98 条典型错误，人工校验修正 67 条"——一个数字胜过长篇描述。

---

## 附录 D · PPT 大纲建议（16 页）

| 页码 | 内容 | 要点 |
| --- | --- | --- |
| 1 | 封面 | 知微 · 会追问的 AI 学习伴侣 |
| 2 | 一句话讲清产品 | 大标题 + 一张归因路径图 |
| 3 | 赛题理解 | 什么叫"教学副驾驶" |
| 4 | 行业痛点 + 与通用 AI 的本质差异 | **v3 强化：结构性差异对比表** |
| 5 | **学习空间结构** | **v3 新增：云盘 → 知识库 → 空间三层图，这是产品骨架** |
| 6 | 核心创新总览 | 四路采集 + 四步闭环全景图 |
| 7 | 四路数据采集 | 漏斗策略 + 证据权重表，讲"数据从哪来" |
| 8 | 错误类型诊断 | 五类错误 → 归因方向映射表，讲"为什么准" |
| 9 | 归因定位（重点页） | 算法流程 + 回溯示意，**这页给足时间** |
| 10 | 掌握度模型 | 加权 BKT 公式与多源对比示例 |
| 11 | 多模态：试卷批量识别 | 链路图 + 实际识别效果，最出效果 |
| 12 | 苏格拉底式引导 + 退出通道 | 一段真实引导对话截屏 |
| 13 | 技术架构 + 数据契约 | 四层架构图 + 九张表关系 + 契约冻结工作流 |
| 14 | 实际界面 | 3–4 张核心页面截图 |
| 15 | 效果验证 | 新题正确率提升 + 教师认可率（**一律实测值**），最有说服力 |
| 16 | 结尾 | 团队、二维码、体验链接、教育理论支撑 |

**演示节奏建议**：把最多时间给第 9 页（归因）和第 15 页（验证）。其余快速带过。**评委记住一个亮点，好过记住十个功能。**

---

*本方案（v4）为「知微」参赛团队工作文档，用于指导开发与材料准备。与四份施工文档（`API_CONTRACT.md` / `PRD.md` / `ALGORITHM.md` / `DATA_SCHEMA.md`）冲突时，**以施工文档为准**。*
