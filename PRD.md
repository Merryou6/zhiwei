# 知微 · PRD（范围与验收）

> 从《参赛完整方案 v3》抽取的产品决策单一来源。只写结论，不写论证。
> 论证与答辩话术留在原方案文档中，本文件不重复。

---

## 0. 一句话定位

知微是基于知识图谱归因的一对一 AI 学习伴侣：**采集证据 → 诊断掌握度 → 定位根因 → 生成处方 → 新题验证**。
首批锚定**初中数学 · 二次函数主线 · 20 个知识点**，平台 LearnBuddy，两人团队。

## 1. 范围红线（MVP 底线，任何讨论不得突破）

1 个默认空间 + 20 个知识点（typical_errors 配满）+ ≥220 道题（train≥5/点 + retest≥6/点，双池隔离）
+ **1 条完整闭环**（自报 → 测评 → 诊断 → 归因 → 处方 → 复测）+ 5 名真实学生数据。

> 任何新功能想法若威胁以上底线，一律进 §4"不做"清单。9/24 功能冻结后一行新代码不写。

## 2. P0 清单（缺一不可，附验收标准）

| # | 功能 | 验收标准（可勾选） |
| --- | --- | --- |
| 1 | 极简认证 | 注册即自动创建"初中数学"默认空间；token 存 localStorage，刷新不掉线 |
| 2 | 自报问卷 | 章节级 5 档 ≤6 题、30 秒完成；提交后 mastery_profiles 有 p_l0 |
| 3 | 测评 | 诊断模式不出已做过的题、不展示对错；收敛即停（规则见 ALGORITHM §2） |
| 4 | 试卷上传 | 上传→识别→逐题确认→批量更新；unclear 项必须手标，无默认值 |
| 5 | 加权 BKT | 三段式更新；幂等（重复提交不重复扣分）；mastery_logs 全留痕；单测过 3 个自检用例 |
| 6 | 错误诊断 | 五类枚举输出；confidence<0.6 退回追问；先复述错误再判定 |
| 7 | 归因定位 | procedural_slip/misreading 不进归因；self 不回溯；upstream 回溯+验证题 |
| 8 | 对话引导 | SSE 流式；2 次答不上给方向提示、3 次给完整解法；连续 3 轮 false 触发退出通道 |
| 9 | 归因结果页 | 展示回溯路径+根因+错误类型+反驳按钮（★答辩主战场，多花时间） |
| 10 | 图谱可视化 | 掌握度四色着色 + 归因路径高亮 + 全局图例 |
| 11 | 复测闭环 | 双池 item_id 零重叠（脚本校验）；报告页展示 ΔAccuracy |
| 12 | 学习报告页 | 掌握度分布 + 缺口清单 + 基线/复测正确率对比 |

## 3. P1（9/23 闭环跑通后才允许碰）

| 功能 | 说明 |
| --- | --- |
| 云盘页 | 预置课标/教材只读列表（后端接口已冻结在契约 §2） |
| 归因反驳完整流 | 反驳按钮 P0 已有；"追加再验证题"闭环 P1 |
| 试卷识别体验打磨 | 切分准确性、识别耗时优化 |
| 知识库上传占位 | 按钮置灰 + "自定义知识库即将开放"（禁用态，不建假文件） |

## 4. 明确不做（及原因）

| 不做 | 原因 |
| --- | --- |
| 独立"拍照搜题"功能页 | 并入对话页：传图读题成功即产生弱负证据（α=0.10） |
| 教师视角演示页（真实页面） | 改为 PPT 静态设计图，省 1.5 天 |
| 第二学科骨架（信息技术） | "引擎无 if(subject) 分支"本身就是可扩展性证明 |
| 报告导出 PDF / 语音提问 | 不贡献闭环 |
| 自定义知识库真实上传 | 底座错误致空间静默失效（方案 4.5），只做禁用占位 |
| 初高衔接 / 高中承接节点 | 跨学段需对齐两套课标，工作量翻倍 |
| 短信验证码 / 密码找回 / JWT 刷新 | 评委不问认证 |
| 并发控制 / 复杂错误码体系 / 全面单测 | 单测只覆盖 BKT 模块 |
| SSE 断线续传 | 降级为非流式 JSON 兜底即可 |
| 掌握度历史快照 | 只存当前值 + mastery_logs 日志 |

## 5. 页面清单（10 个）

| # | 页面 | 核心任务 | 优先级 |
| --- | --- | --- | --- |
| 1 | 登录/注册 | 一件事：进得来 | P0 |
| 2 | 自报问卷 | 30 秒建先验 + 定主攻章节 | P0 |
| 3 | 空间列表 | 默认已有空间；新建是次要按钮 | P0 |
| 4 | 测评页 | 单题呈现 + 进度条，无对错反馈 | P0 |
| 5 | 试卷上传页 | 识别进度 → 逐题确认对错 | P0 |
| 6 | 对话辅导页 | 引导对话 + 传图读题 + 退出提示 | P0 |
| 7 | 归因结果页 | 路径 + 根因 + 错误类型 + 反驳 | P0 |
| 8 | 知识图谱页 | 掌握度着色 + 路径高亮（兼做"学习路径页"） | P0 |
| 9 | 学习报告页 | 掌握度分布 + ΔAccuracy | P0 |
| 10 | 云盘页 | 预置教材列表 | P1 |

设计原则：一屏一件事；第一屏就让用户开始；采集永不弹窗（静默证据仅非阻断气泡）；空间不占首屏。

## 6. 交互纪律

**语气：像耐心的学长，不像评判者。**

| 场景 | 不说 | 说 |
| --- | --- | --- |
| 知识点薄弱 | 你掌握很差 | 这一环还有点晃，我们再稳一下 |
| 答错了 | 回答错误 | 这个坑很常见，我们看看它是怎么来的 |
| 归因成功 | 检测到知识缺陷 | 找到啦——真正卡住你的是这里 |
| 触发退出 | 你答不上来 | 我们先往回看一眼 XX，那里可能是关键 |

**颜色语义（全局一致 + 图例）**：`<0.4` 暖橙（待巩固）/ `0.4–0.6` 黄（不稳定）/ `0.6–0.8` 浅青绿（基本掌握）/ `>0.8` 青绿（已掌握）。主色低饱和青蓝；**禁用刺眼大红**。

## 7. 效果验证（两项硬指标）

| 指标 | 设计 | 判据 |
| --- | --- | --- |
| 新题正确率 | 每生基线 **3 题**（retest 池）→ 干预 → 复测 **3 题**（另外的 retest 池题，与基线题不重复）；与训练池零重叠 | ΔAccuracy ≥ 0.3 且复测 ≥ 0.75 |
| 归因认可率 | 5 名学生（招募顺利扩至 8–10），≥15 条归因记录，1–2 位一线教师判定"合理/部分合理/不合理" | ≥75%（冲刺 80%） |

- 全部样本落在**二次函数同一章节**（样本同质，教师审得快、认得准）。
- **PPT 中一律使用实测值**。方案文档里的"33% → 80%"是示例，禁止照搬。

## 8. 里程碑（每天必交一个可演示版本）

| 日期 | 甲（诊断线） | 乙（教学线） | 当日必交 |
| --- | --- | --- | --- |
| 9/19 | 表结构草案 + 招 3 名学生 | 脚手架 + 部署通路 | **契约冻结** + 学生名单 |
| 9/20 | 图谱 20 节点 + 错误库初稿 | 空间页 + 认证 | 能登录、有默认空间 |
| 9/21 | 题库 ≥220 题 + BKT + 单测 | 测评页 + 试卷确认页 | 自报→测评全链路 |
| 9/22 | 错误诊断 + 归因可演示 | 对话 + SSE + 退出通道 | 归因演示 |
| 9/23 | 归因联调 + 复测闭环 | 图谱页 + 报告页 | **完整闭环跑通** |
| 9/24 | **功能冻结**；5 生实测记录 | 演示打磨 + Demo 草稿 | 公网链接 + 实测数据 |
| 9/25 | 教师审阅 + 数据成表 | Demo 视频 + PPT | 三项材料齐备 |
| 9/26 | 对练答辩 + **上午提交** | 同左 | 提交完毕 |

## 9. 分工（纵切，不拆前后端）

- **成员甲 · 诊断线（全栈）**：知识图谱 / 错误库 / 题库校验、BKT 引擎、错误诊断、归因算法、测评与试卷接口、学生实验执行
- **成员乙 · 教学线（全栈）**：脚手架 / 认证 / 部署、登录注册页 / 空间页 / 测评页 / 试卷确认页、对话 + SSE + 退出通道、归因结果页 / 图谱页 / 报告页、Demo 视频 + PPT
- 协作纪律：每日 30 分钟同步；接口争议**以 API_CONTRACT.md 为准**；契约修改需双方同意 + 变更记录；每完成一个模块立刻 commit（留痕即"AI 工具使用"评分材料）
