node_modules/
dist/
build/
functions/api/dist/
apps/web/dist/
data/local_db/
.DS_Store
.learnbuddy/
*.log
coverage/
.vite/
__pycache__/
*.pyc

# 环境变量文件：承载 ZHIWEI_LLM_API_KEY / ZHIWEI_SERVER_SECRET 等机密。
# 机密只走运行时注入（进程环境变量 / 部署平台配置），绝不入库。
.env
.env.*
!.env.example
# 部署件（deploy/ 目录）里的真值 env 一律不入库（通配挡住将来新增的 deploy/prod.env 等）；
# 模板仍可入库（*.env.example 例外）
deploy/*.env
!deploy/*.env.example
